{
  "name": "template",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "babel src --out-dir dist --experimental",
    "test": "mocha --compilers js:babel/register test/**/*.spec.js",
    "lint": "eslint src",
    "doc": "jsdoc -r -d docs src --verbose -R README.md",
    "dev": "npm run lint && npm run test && npm run build && npm start"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "babel": "^5.6.14",
    "babel-eslint": "^3.1.23",
    "chai": "^3.0.0",
    "esdoc": "^0.1.3",
    "eslint": "^0.24.0",
    "mocha": "^2.2.5",
    "sinon": "^1.15.4",
    "sinon-chai": "^2.8.0"
  }
}
